services:
  - type: web
    name: ecomm-api
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn ecomm.wsgi:application"
    rootDir: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: full_stack_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".render.com .onrender.com"

  - type: web
    name: ecomm-frontend
    runtime: static
    rootDir: frontend
    buildCommand: "npm install && npm run build"
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: ecomm-api
          property: url

databases:
  - name: full_stack_db
    plan: free
